import React from 'react';

import { PlaceholderScreen } from '../components';

export default function AICoachScreen() {
  return (
    <PlaceholderScreen
      eyebrow="AI Coach"
      subtitle="Esta pantalla se construye en su propio turno, una vez confirmemos la forma exacta del JSON que devuelve GET /ai/insights."
      title="Diagnóstico IA"
    />
  );
}
