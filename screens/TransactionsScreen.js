import React from 'react';

import { PlaceholderScreen } from '../components';

export default function TransactionsScreen() {
  return (
    <PlaceholderScreen
      eyebrow="Órbitas"
      subtitle="Esta pantalla se construye en su propio turno, una vez confirmemos los campos exactos que devuelve GET /transactions."
      title="Transacciones"
    />
  );
}
